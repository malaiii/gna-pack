/** Settings for test file, maintaining the global configs for test frame work. */
import React from 'react';

/** importing react and cleanup function will reduce the statements in each test file*/
global.React = React;